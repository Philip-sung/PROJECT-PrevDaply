import styled from 'styled-components/native';
import { View } from 'react-native-ui-lib';

export const CreatorWrapper = styled(View)`
  flex-direction: row;
  align-items: center;
`;

export const CreatorContent = styled(View)`
  flex-direction: row;
`;

export const CreatorInfo = styled(View)`
  margin-left: 10px;
`;
